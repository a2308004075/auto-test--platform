# Redis 开发环境配置
# 关闭 RDB 和 AOF 持久化，避免 Windows 非管理员/权限不足导致 MISCONF 写入保护

bind 127.0.0.1
port 6379
protected-mode yes

# 禁用 RDB 快照
save ""

# 禁用 AOF
appendonly no

# 即使持久化异常也不阻止写入
stop-writes-on-bgsave-error no
