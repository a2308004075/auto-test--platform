-- ============================================================
-- V30 侧边栏重组：拆分测试管理目录，新增关键字管理目录
-- 目标结构：
--   项目菜单
--   ├── 仪表盘（原「仪表板」，重命名）
--   ├── 环境配置（从测试管理提升为顶级菜单）
--   ├── 接口管理（从测试管理提升为顶级菜单）
--   ├── 关键字管理（新目录）
--   │   ├── 接口关键字
--   │   ├── 工具方法
--   │   └── Action
--   └── 测试管理（保留目录，仅含测试相关页面）
--       ├── 测试用例
--       ├── 测试套件
--       ├── 测试计划
--       └── 执行记录
-- ============================================================

SET NAMES utf8mb4;

-- ============================================================
-- 1. 重命名「仪表板」→「仪表盘」
-- ============================================================
UPDATE `sys_menu` SET `name` = '仪表盘' WHERE `id` = 11;

-- ============================================================
-- 2. 将「环境配置」「接口管理」从测试管理目录提升为项目菜单顶级项
-- ============================================================
UPDATE `sys_menu` SET `parent_id` = 10, `sort_no` = 1 WHERE `id` = 13; -- 环境配置
UPDATE `sys_menu` SET `parent_id` = 10, `sort_no` = 2 WHERE `id` = 12; -- 接口管理

-- ============================================================
-- 3. 新增「关键字管理」目录（parent_id=10, menu_type=1, sort_no=3）
-- ============================================================
INSERT INTO `sys_menu` (`name`, `parent_id`, `menu_type`, `icon`, `route_path`, `component`, `sort_no`)
VALUES ('关键字管理', 10, 1, '', '', NULL, 3);

-- ============================================================
-- 4. 将「接口关键字」「工具方法」「Action」移入关键字管理目录
-- ============================================================
UPDATE `sys_menu` SET `parent_id` = LAST_INSERT_ID(), `sort_no` = 1 WHERE `id` = 14; -- 接口关键字
UPDATE `sys_menu` SET `parent_id` = LAST_INSERT_ID(), `sort_no` = 2 WHERE `id` = 15; -- 工具方法
UPDATE `sys_menu` SET `parent_id` = LAST_INSERT_ID(), `sort_no` = 3 WHERE `id` = 16; -- Action

-- ============================================================
-- 5. 更新「测试管理」目录排序号（排在关键字管理之后）
-- ============================================================
UPDATE `sys_menu` SET `sort_no` = 4 WHERE `name` = '测试管理' AND `parent_id` = 10 AND `menu_type` = 1;

-- ============================================================
-- 6. 重新排序测试管理目录下保留的 4 个菜单项
-- ============================================================
UPDATE `sys_menu` SET `sort_no` = 1 WHERE `id` = 18; -- 测试用例
UPDATE `sys_menu` SET `sort_no` = 2 WHERE `id` = 17; -- 测试套件
UPDATE `sys_menu` SET `sort_no` = 3 WHERE `id` = 19; -- 测试计划
UPDATE `sys_menu` SET `sort_no` = 4 WHERE `id` = 20; -- 执行记录
