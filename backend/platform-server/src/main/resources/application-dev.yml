# 开发环境配置

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auto_test_platform?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: pp2024

  redis:
    host: localhost
    port: 6379
    timeout: 5000

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123
    virtual-host: /

  flyway:
    validate-on-migrate: true
    clean-disabled: false

# JWT 密钥（开发环境，生产环境必须更换）
jwt:
  secret: dev-secret-key-auto-test-platform-2024-change-in-production

# 日志级别与文件输出由 logback-spring.xml 统一管理

# 关闭 SQL 详情输出，由 logback-spring.xml 统一管理
# MyBatis-Plus 日志实现继承 application.yml 的 Slf4jImpl
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
