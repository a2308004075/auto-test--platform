-- =====================================================================
-- V37: 将【关键字管理】提升为项目菜单下的一级目录
-- 按图调整左侧菜单树：
--   1. 把【关键字管理】从【自动化测试】目录下移出；
--   2. 放到【项目菜单】下，与【测试管理】同级，并位于【测试管理】上方。
-- =====================================================================

-- ── 1. 将关键字管理(id=79) 的父级改为项目菜单(id=10)，排序置于测试管理之上 ──
UPDATE `sys_menu` SET `parent_id` = 10, `sort_no` = 3, `updated_at` = NOW() WHERE `id` = 79;

-- ── 2. 将测试管理(id=78) 的排序下移，确保关键字管理位于其上方 ──
UPDATE `sys_menu` SET `sort_no` = 4, `updated_at` = NOW() WHERE `id` = 78;

-- ── 3. 关键字管理下子菜单顺序保持不变（接口关键字 / 工具方法 / Action关键字）──
-- 14 接口关键字 / 15 工具方法 / 16 Action关键字

-- ── 4. 自动化测试目录下移除关键字管理后，重新调整子菜单排序 ──
UPDATE `sys_menu` SET `sort_no` = 1, `updated_at` = NOW() WHERE `id` = 18;  -- 自动化用例
UPDATE `sys_menu` SET `sort_no` = 2, `updated_at` = NOW() WHERE `id` = 17;  -- 自动化套件
UPDATE `sys_menu` SET `sort_no` = 3, `updated_at` = NOW() WHERE `id` = 20;  -- 执行记录
