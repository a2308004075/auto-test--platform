# 开发环境配置

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/postman_platform?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: pp2024

  redis:
    host: localhost
    port: 6379
    timeout: 5000

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123
    virtual-host: /

  flyway:
    validate-on-migrate: true
    clean-disabled: false

# JWT 密钥（开发环境，生产环境必须更换）
jwt:
  secret: dev-secret-key-postman-platform-2024-change-in-production

# 开发环境日志级别
logging:
  level:
    root: INFO
    com.postman.platform: DEBUG
    org.springframework.web: INFO

# 显示 SQL 日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
